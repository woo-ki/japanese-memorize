import { Theme } from '@emotion/react';
import { colors } from '@styles/theme/colors.emotion.ts';
import { utils } from '@styles/theme/utils.emotion.ts';
import { layouts } from '@styles/theme/layouts.emotion.ts';

export const theme: Theme = {
  colors,
  utils,
  layouts,
};
